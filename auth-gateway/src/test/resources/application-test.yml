spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: mock
    password: mock
    driver-class-name: org.h2.Driver
    platform: h2
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect

admin:
  token: ______mock______
